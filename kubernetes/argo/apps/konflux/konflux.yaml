---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: konflux
  namespace: argo-system
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  project: kubernetes
  sources:
    - repoURL: "https://github.com/skel84/home-ops.git"
      path: kubernetes/apps/konflux/konflux
      targetRevision: main
  destination:
    name: in-cluster
    namespace: konflux-operator
  syncPolicy:
    automated:
      allowEmpty: true
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
