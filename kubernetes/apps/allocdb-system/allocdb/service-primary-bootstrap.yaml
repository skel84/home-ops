apiVersion: v1
kind: Service
metadata:
  name: allocdb-primary-bootstrap
  namespace: allocdb-system
  labels:
    app.kubernetes.io/name: allocdb
spec:
  selector:
    statefulset.kubernetes.io/pod-name: allocdb-0
  ports:
    - name: client
      port: 18000
      targetPort: client
