---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./examples-disabled-configmap.yaml
  # VM examples stay on disk here, but are intentionally not managed by Argo by default.
  # Uncomment one example pair locally when you actually want to boot it.
  # - ./vm-ubuntu-example.yaml
  # - ./vm-ubuntu-example-ssh-service.yaml
  # - ./vm-cirros-baseline.yaml
  # - ./vm-cirros-baseline-ssh-service.yaml
  # - ./vm-freebsd-example.yaml
  # - ./vm-freebsd-example-ssh-service.yaml
